import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "VDOgate Ambassador Program - India's Exclusive Creator Economy Leadership",
  description: "Join India's Most Exclusive Creator Economy Leadership Program. Become a VDOgate Ambassador and be part of the future of digital content creation.",
  robots: "index, follow",
  keywords: "VDOgate, Ambassador, Creator Economy, India, Leadership Program, Digital Content",
};

export const viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
